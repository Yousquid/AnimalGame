# Animal Game — HANDOFF 使用规则

这个文件不再承担持续记录每次开发改动的职责，也不是执行普通任务前必须通读的项目说明。

当前完整项目快照已冻结归档在 [`ANIMALGAME_HANDOFF_ARCHIVE_2026-08-12.md`](./ANIMALGAME_HANDOFF_ARCHIVE_2026-08-12.md)。它只用于历史追溯或冷启动参考，不代表代码的实时状态；代码、Prefab 和场景始终是当前事实来源。

## 默认工作方式

- 除非用户明确要求，否则不要读取归档 HANDOFF，也不要更新本文件或归档。
- 普通任务只检查与需求直接相关的脚本、Prefab、场景和必要调用链；不要在每次修改前重新遍历整个项目。
- 从精准定位开始，只有发现真实依赖或风险时才扩大检查范围。
- 验证强度与改动范围相匹配：小改动做定向检查，跨系统或高风险改动才做更广泛的编译与回归检查。
- 默认由主智能体直接完成任务。只有工作能够真正独立并行、且预计会缩短总耗时时，才考虑使用最多一个子智能体；不要让多个智能体重复浏览项目或重复做同一套架构审查。
- 过程更新保持简短，以实现、验证和结果为主；除非用户要求，不先输出冗长方案。

## 何时使用归档

仅在以下情况下参考归档，并优先只读相关章节：

- 用户明确要求查看历史背景或读取 HANDOFF；
- 新任务缺少关键背景，而且无法从相关代码和 Prefab 中可靠确认；
- 用户明确要求更新、重建或交接 HANDOFF。

任何功能修改完成后，都不要自动把改动追加进 HANDOFF。只有收到用户的明确要求时才更新。
